using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LeqentBtn : MonoBehaviour
{
    HeroConfig config;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void SetLeqentInfo(HeroConfig c)
    {
        config = c;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
